version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: postgres-db
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: alias_db
      POSTGRES_PASSWORD: alias_db
      POSTGRES_DB: alias_db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
